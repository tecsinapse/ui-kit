import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { UploaderStory } from '../utils/UploaderStory';
import { Button } from '@tecsinapse/ui-kit';
import { DivFlex } from '@tecsinapse/ui-kit';
import { Uploader } from './Uploader';
import { FormUploader } from './FormUploader/FormUploader';
import { useRef } from 'react';

<Meta title="Packages @tecsinapse/uploader" decorators={[story => <DivFlex>{story()}</DivFlex>]} />

# Uploader

The `Uploader` component can receive the following props:

<Props components={{ Uploader, FormUploader }} />

# Code snippets

Here you can check the code snippet for the story.

## Uploader drag

<Story name="Uploader drag" parameters={{ docs: { disable: true } }}>
  <div style={{ width: '700px', height: '400px' }}>
    <UploaderStory />
  </div>
</Story>

<Source id="packages-tecsinapse-uploader--uploader-drag" />

## Uploader form

<Story name="Uploader form" parameters={{ docs: { disable: true } }}>
  <div style={{ width: '800px', height: '500px' }}>
    <UploaderStory type="form" />
  </div>
</Story>

<Source id="packages-tecsinapse-uploader--uploader-form" />

## Uploader silent

<Story name="Uploader silent" parameters={{ docs: { disable: true } }}>
  {() => {
    const fancyRef = useRef();
    return (
      <div>
        <Button
          onClick={() => {
            fancyRef.current.open();
          }}
          customVariant="success"
          variant="contained"
        >
          Upload
        </Button>
        <UploaderStory silent ref={fancyRef} />
      </div>
    );
  }}
</Story>

<Source id="packages-tecsinapse-uploader--uploader-silent" />
