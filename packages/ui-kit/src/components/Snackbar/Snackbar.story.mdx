import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { Snackbar } from './Snackbar';
import { GROUPS } from '../../../../../.storybook/hierarchySeparators';
import { DivFlex } from '../../withFlexCenter';

<Meta
  title={`${GROUPS.NOTIFICATIONS}|Snackbar`}
  decorators={[story => <DivFlex>{story()}</DivFlex>]}
/>

# Snackbar

The `Snackbar` component can receive the following props:

<Props of={Snackbar} />

# Code snippets

Here you can check the code snippet for the story.

## Notification error

<Story name="Notification error" parameters={{ docs: { disable: true } }}>
  <Snackbar show variant="error">
    Test Message
  </Snackbar>
</Story>

<Source id="notifications-snackbar--notification-error" />

## Notification success

<Story name="Notification success" parameters={{ docs: { disable: true } }}>
  <Snackbar show variant="success">
    Test Message
  </Snackbar>
</Story>

<Source id="notifications-snackbar--notification-success" />

## Notification warning

<Story name="Notification warning" parameters={{ docs: { disable: true } }}>
  <Snackbar show variant="warning">
    Test Message
  </Snackbar>
</Story>

<Source id="notifications-snackbar--notification-warning" />
