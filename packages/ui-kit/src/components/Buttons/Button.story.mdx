import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { useState } from 'react';
import Icon from '@mdi/react';
import { IconButton } from './IconButton';
import { FloatingButton } from './FloatingButton';
import { FloatingButtonList } from './FloatingButtonList';
import { DivButton } from './DivButton';
import { Button } from './Button';
import { DivFlex } from '../../withFlexCenter';
import { GROUPS } from '../../../../../.storybook/hierarchySeparators';
import FileCopyRounded from '@material-ui/icons/FileCopyRounded';
import DeleteIcon from '@material-ui/icons/Delete';
import { mdiFolderPlus } from '@mdi/js';
import { handleClick, items } from './storyHelper';

<Meta
  title={`${GROUPS.COMPONENTS}|Button`}
  decorators={[story => <DivFlex>{story()}</DivFlex>]}
/>

# Button

Each Button component can receive the following props:

<Props
  components={{
    Button,
    IconButton,
    FloatingButton,
    DivButton,
  }}
/>

# Code snippets

Here you can check the code snippet for the story.

## Button

<Story name="Button" parameters={{ docs: { disable: true } }}>
  <Button onClick={handleClick}>Smart Button</Button>
</Story>

<Source id="components-button--button" />

## Default

<Story name="Default" parameters={{ docs: { disable: true } }}>
  <Button customVariant="default" onClick={handleClick}>
    Smart Button
  </Button>
</Story>

<Source id="components-button--default-story" />

## Success submitting

<Story name="Success submitting" parameters={{ docs: { disable: true } }}>
  {() => {
    const [submitting, setSub] = useState(false);

    return (
      <Button
        customVariant="success"
        onClick={() => setSub(true)}
        submitting={submitting}
      >
        Smart Button
      </Button>
    );
  }}
</Story>

<Source id="components-button--success-submitting" />

## Warning

<Story name="Warning" parameters={{ docs: { disable: true } }}>
  <Button customVariant="warning" onClick={handleClick}>
    Smart Button
  </Button>
</Story>

<Source id="components-button--warning" />

## Error

<Story name="Error" parameters={{ docs: { disable: true } }}>
  <Button customVariant="error" onClick={handleClick}>
    Smart Button
  </Button>
</Story>

<Source id="components-button--error" />

## Primary

<Story name="Primary" parameters={{ docs: { disable: true } }}>
  <Button color="primary" onClick={handleClick}>
    Smart Button
  </Button>
</Story>

<Source id="components-button--primary" />

## Primary contained

<Story name="Primary contained" parameters={{ docs: { disable: true } }}>
  <Button color="primary" variant="contained">
    <Icon path={mdiFolderPlus} size={1} color="white" />
    Nova Pasta
  </Button>
</Story>

<Source id="components-button--primary-contained" />

## Secondary

<Story name="Secondary" parameters={{ docs: { disable: true } }}>
  <Button color="secondary" onClick={handleClick}>
    Smart Button
  </Button>
</Story>

<Source id="components-button--secondary" />

## Icon

<Story name="Icon" parameters={{ docs: { disable: true } }}>
  <IconButton onClick={handleClick}>
    <DeleteIcon />
  </IconButton>
</Story>

<Source id="components-button--icon" />

## Floating

<Story name="Floating" parameters={{ docs: { disable: true } }}>
  <FloatingButton onClick={handleClick}>
    <DeleteIcon />
  </FloatingButton>
</Story>

<Source id="components-button--floating" />

## FloatingList

<Story name="Floating List" parameters={{ docs: { disable: true } }}>
    {
        () => {
            const [open, setOpen] = useState(true);

            return (<FloatingButtonList onClick={() => setOpen(!open)} open={open}
                  items={items}
            />);
        }
    }
</Story>

<Source id="components-button--floating-list" />

## Div

<Story name="Div" parameters={{ docs: { disable: true } }}>
  <div
    style={{
      width: '100vw',
      height: '160px',
      backgroundColor: '#e0e0e0',
    }}
  >
    <DivButton onClick={handleClick} infoText="COMUNICADOS" notifyNumber={0}>
      <FileCopyRounded fontSize="large" />
    </DivButton>
  </div>
</Story>

<Source id="components-button--div" />
