import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { GROUPS } from '../../../../../.storybook/hierarchySeparators';
import { Divider } from '../..';
import { DivFlex } from '../..';

<Meta
  title={`${GROUPS.LAYOUT}/Divider`}
  decorators={[
    story => (
      <DivFlex>
        <div style={{ margin: 'auto', width: '50%', paddingTop: '5vh' }}>
          {story()}
        </div>
      </DivFlex>
    ),
  ]}
/>

# Divider

The `Divider` component can receive the following props:

<Props of={Divider} />

# Code snippets

Here you can check the code snippet for the story.

## Solid

<Story name="Solid" parameters={{ docs: { disable: true } }}>
  <Divider variant="solid" />
</Story>

<Source id="layout-divider--solid" />

## Dashed

<Story name="Dashed" parameters={{ docs: { disable: true } }}>
  <Divider variant="dashed" />
</Story>

<Source id="layout-divider--dashed" />
