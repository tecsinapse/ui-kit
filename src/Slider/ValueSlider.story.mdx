import { Meta, Story, Props, Source } from '@storybook/addon-docs/blocks';
import { useState } from 'react';
import { ValueSlider } from './ValueSlider';
import { GROUPS } from '../../.storybook/hierarchySeparators';
import { DivFlex } from '../withFlexCenter';
import { defaultOrange } from '../colors';

<Meta
  title={`${GROUPS.COMPONENTS}|Value Slider`}
  decorators={[story => <DivFlex>{story()}</DivFlex>]}
/>

# Value Slider

The `ValueSlider` component can receive the following props:

<Props of={ValueSlider} />

# Code snippets

Here you can check the code snippet for the story.

## Interval slider

<Story name="Simple slider" parameters={{ docs: { disable: true } }}>
  {() => {
    const marks = [
      {
        value: 0,
        label: '0',
      },
      {
        value: 3,
        label: '3',
      },
      {
        value: 5,
        label: '5',
      },
    ];
    return (
      <div style={{ width: '75%', marginTop: '5vh' }}>
        <ValueSlider
          colorSlider={defaultOrange}
          value="3"
          defaultValue="10"
          step={2}
          marks={marks}
          min={0}
          max={5}
        />
      </div>
    );
  }}
</Story>

<Source id="components-value-slider--simple-slider" />
