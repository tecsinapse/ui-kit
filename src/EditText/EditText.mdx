---
name: EditText
category: EditText
group: EditText
---

import EditText from './EditText';
import { Playground, Props } from 'docz';

# EditText

<Props of={EditText} />

## Basic usage

### The component requires that the files from `https://github.com/tinymce/tinymce-dist/tree/master/skins/ui/oxide`
### should be copied to the `public` folder and passed as a prop to 'skinUrl' (start as '/' meaning the public folder)

<EditText />
